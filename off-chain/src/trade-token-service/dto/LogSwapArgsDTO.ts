export interface LogSwapArgsDTO {
    inputToken: string;
    inAmount: string;
    outputToken: string;
    outAmountUsd: string;
    outAmount: string;
    txId: string;
    timestamp: string;
    isBuyToken: string;
}